# Optional. Destination address for contact-form submissions.
# Without it, the form handler logs submissions server-side instead of sending mail.
CONTACT_FORWARD_EMAIL=

# Optional. Public site URL, used for metadata, canonical links and the sitemap.
NEXT_PUBLIC_SITE_URL=https://aleksandrsletin.com
